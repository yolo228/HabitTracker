<Window x:Class="HabitTracker.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация" Height="350" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Создание аккаунта" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Margin="0,5" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,5"/>
        <PasswordBox x:Name="ConfirmBox" Grid.Row="3" Margin="0,5"/>
        <TextBox x:Name="EmailBox" Grid.Row="4" Margin="0,5"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Зарегистрировать" Width="120" Margin="5" Click="Register_Click"/>
            <Button Content="Отмена" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
        <TextBlock x:Name="MessageText" Grid.Row="6" Foreground="Red" HorizontalAlignment="Center"/>
    </Grid>
</Window>